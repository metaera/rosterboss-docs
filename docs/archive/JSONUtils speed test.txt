var o1 = {
  f1: 45,
  f2: "Test",
  f3: "Test",
  f4: "Test",
  f5: "Test",
  f6: "Test",
  f7: "Test",
  f8: "Test",
  f100: {
    a1: [
      45, 23, 46, 12, 67, 100, 35, 12, 56, 100, {
        b1: [56, 46, 23, 45, 12],
        b2: "This is nice"
      }
    ]
  }
};

console.time('Angular Copy');
for (var i = 0; i < 100; ++i)
  var o2 = angular.copy(o1);
console.timeEnd('Angular Copy');

console.log("Objects are the same: " + JSONUtils.compare(o1, o2) + "\n");

console.time('Angular Update');
for (i = 0; i < 100; ++i)
  angular.copy(o2,o1);
console.timeEnd('Angular Update');

console.log("Objects are the same: " + JSONUtils.compare(o1, o2) + "\n");

console.time('JSONUtils Copy');
for (i = 0; i < 100; ++i)
  o2 = JSONUtils.deepClone(o1);
console.timeEnd('JSONUtils Copy');

console.log("Objects are the same: " + JSONUtils.compare(o1, o2) + "\n");

console.time('JSONUtils Update');
for (i = 0; i < 100; ++i)
  JSONUtils.updateObj(o2,o1);
console.timeEnd('JSONUtils Update');

console.log("Objects are the same: " + JSONUtils.compare(o1, o2) + "\n");
