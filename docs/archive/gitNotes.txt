git rm --cached <filename>
git rm --cached bin/com/direct/\*     <--- need to escape the *
git commit -m'file removed'

// This will make files invisible in the repo but keep them there until restored with --no-assume-unchanged
// This is for files that change frequently but are checked in rarely
git update-index --assume-unchanged <path>

Global rules for all projects can go into a file specified as follows:

    git config --global core.exclusesfile ~/.gitignore_global

Ignore rules to share with others go in:
    .gitignore

Local Exclusions go into:

    .git/info/exclude

